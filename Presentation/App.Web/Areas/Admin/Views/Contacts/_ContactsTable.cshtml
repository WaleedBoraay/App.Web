@model List<App.Web.Areas.Admin.Models.Registrations.ContactModel>

<div class="d-flex justify-content-between align-items-center mb-2">
    <h4>Contacts</h4>
    <a asp-controller="Contacts" asp-action="Create" asp-route-registrationId="@ViewData["RegistrationId"]" class="btn btn-success">
        + Add Contact
    </a>
</div>

@if (Model != null && Model.Any())
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Phone</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var c in Model)
            {
                <tr>
                    <td>@c.FirstName @c.LastName</td>
                    <td>@c.Email</td>
                    <td>@c.ContactPhone</td>
                    <td>
                        <a asp-controller="Contacts" asp-action="Edit" asp-route-id="@c.Id" class="btn btn-warning">Edit</a>
                        <form asp-controller="Contacts" asp-action="Delete" method="post" asp-route-id="@c.Id" asp-route-registrationId="@c.RegistrationId" style="display:inline;">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No contacts available.</p>
}
